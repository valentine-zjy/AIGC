{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node --experimental-strip-types src/main.ts",
    "test": "node --experimental-strip-types src/worker.test.mjs"
  },
  "dependencies": {
    "@ai-rewrite/contracts": "0.1.0",
    "@ai-rewrite/db": "0.1.0",
    "@ai-rewrite/queue": "0.1.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.3"
  }
}
